-- HAND SQL v0.1 (generated)
-- module: demo

-- DDL
CREATE TABLE IF NOT EXISTS users (
  id INTEGER NOT NULL,
  email TEXT NOT NULL,
  age INTEGER,
  is_admin BOOLEAN NOT NULL,
  PRIMARY KEY (id)
);


-- DML
