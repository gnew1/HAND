🎬 PROGRAMA:
    show "CRUD intent"
🛠 create_user(id: Int, name: Text) -> Null:
    show "INSERT users"
    return null
🛠 get_user(id: Int) -> Text:
    show "SELECT users"
    return "user"
create_user(1, "ana")
show get_user(1)
